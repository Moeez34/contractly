import './Footer.css';

const Footer = () => {
    return (
        <footer className="app-footer">
            <p className="footer-text">Keep building. Keep working.</p>
        </footer>
    );
};

export default Footer;
